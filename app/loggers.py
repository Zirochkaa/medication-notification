import logging


handlers_callbacks_log = logging.getLogger("handlers.callbacks")
handlers_commands_log = logging.getLogger("handlers.commands")

scheduler_helpers_log = logging.getLogger("scheduler.helpers")
scheduler_tasks_log = logging.getLogger("scheduler.tasks")

exception_handlers_log = logging.getLogger("exception_handlers")
logs_notifications_log = logging.getLogger("logs_notifications")
run_log = logging.getLogger("run")
